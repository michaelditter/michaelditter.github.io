# Buttondown API key for newsletter subscriptions
BUTTONDOWN_API_KEY=your_buttondown_api_key_here

# Email delivery configuration (SMTP)
SMTP_HOST=smtp.example.com
SMTP_PORT=465
SMTP_SECURE=true
SMTP_USER=your_email@example.com
SMTP_PASS=your_email_password_or_app_password
EMAIL_FROM_NAME=Michael J Ditter

# Copy this file to .env and update values with your credentials
# DO NOT commit the .env file to git 